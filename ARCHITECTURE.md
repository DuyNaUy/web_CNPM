# 🔄 System Architecture Diagram

## Complete Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         User Interface                          │
│                                                                 │
│         ┌──────────────────────────────────────┐               │
│         │  /customer/ai-agent Page             │               │
│         │  ├── AIAgentConsole (Wrapper)        │               │
│         │  │   ├── AIAgentChat (Chat UI)       │               │
│         │  │   └── OrderPreview (Order UI)     │               │
│         └──────────────────────────────────────┘               │
└─────────────────────────────────────────────────────────────────┘
                           ↓↑
                    HTTP/WebSocket
                    (JWT Tokens)
                           ↓↑
┌─────────────────────────────────────────────────────────────────┐
│                     Django REST API                             │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           /api/ai/ Routes (6 endpoints)                  │  │
│  │                                                          │  │
│  │  ┌─────────────────────┐    ┌──────────────────────┐    │  │
│  │  │ ConversationViewSet │    │  OrderViewSet        │    │  │
│  │  │ ├─ start_conv...    │    │  ├─ confirm_create  │    │  │
│  │  │ ├─ send_message     │    │  ├─ cancel          │    │  │
│  │  │ ├─ get_history      │    │  └─ create_from...  │    │  │
│  │  │ └─ close_conv...    │    │                      │    │  │
│  │  └─────────────────────┘    └──────────────────────┘    │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
         ↓↑                  ↓↑                  ↓↑
        User          AI Agent Service     Auto Order Creation
     Management
         ↓↑                  ↓↑                  ↓↑
┌─────────────────────────────────────────────────────────────────┐
│                  Service Layer (Business Logic)                 │
│                                                                 │
│     ┌─────────────────────────────────────────────────────┐    │
│     │  AIAgentService                                     │    │
│     │                                                     │    │
│     │  ├─ start_conversation()                           │    │
│     │  ├─ chat(conversation, message)                    │    │
│     │  │  ├─ OpenAI API (if key available)              │    │
│     │  │  └─ Fallback: Keyword matching                 │    │
│     │  ├─ _call_openai_api()                            │    │
│     │  ├─ _fallback_response()                          │    │
│     │  ├─ _match_keywords_to_products()                 │    │
│     │  └─ create_order_from_recommendations()           │    │
│     │                                                     │    │
│     └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                 ↓↑                        ↓↑
            Database                    External
            Queries                    AI APIs
                 ↓↑                        ↓↑
┌──────────────────────────┐    ┌────────────────────────┐
│    Django Models         │    │   OpenAI / Fallback    │
│                          │    │                        │
│ ├─ ConversationSession   │    │ ├─ gpt-3.5-turbo       │
│ ├─ AIRecommendation      │    │ ├─ Context awareness   │
│ ├─ AutomatedOrder        │    │ └─ Error handling      │
│ ├─ Product (existing)    │    │                        │
│ ├─ Order (existing)      │    └────────────────────────┘
│ └─ User (existing)       │
└──────────────────────────┘
         ↓↑
┌──────────────────────────┐
│   MySQL Database         │
│                          │
│ ├─ ai_agent_*            │
│ ├─ products_*            │
│ ├─ orders_*              │
│ ├─ users_*               │
│ └─ categories_*          │
└──────────────────────────┘
```

---

## Data Flow: Chat Message

```
User Types Message
        ↓
    Client
        ↓
  JavaScript
  fetch() POST /api/ai/conversations/{id}/send_message/
        ↓
   With JWT Token
        ↓
  Django Router
        ↓
  ConversationViewSet.send_message()
        ↓
  AIAgentService.chat()
        ↓
    ┌─ Has OPENAI_API_KEY?
    │  ├─ YES → Call OpenAI API
    │  │         ├─ Get gpt-3.5-turbo response
    │  │         ├─ Parse recommendations
    │  │         └─ Return formatted response
    │  │
    │  └─ NO → Use Fallback
    │          ├─ Match keywords to products
    │          ├─ Get top-selling products
    │          └─ Return recommendations
    ↓
Save to Database
  ├─ Add message to ConversationSession.context
  ├─ Create AIRecommendation records
  └─ Save to ai_agent_* tables
    ↓
Return JSON Response
  ├─ ai_response (text)
  ├─ recommendations (array)
  └─ should_create_order (boolean)
    ↓
  Frontend
    ├─ Display message
    ├─ Show recommendations
    └─ Enable "Create Order" button
    ↓
  User Sees Results
```

---

## Data Flow: Order Creation

```
User Clicks "Create Order"
        ↓
 Send product IDs + quantities
        ↓
  Backend API
  OrderViewSet.confirm_and_create()
        ↓
  Find AutomatedOrder record
        ↓
  Validate status = 'draft'
        ↓
  Create Order record
  (links to user, calculates total)
        ↓
  Create CartItem records
  (for each product in recommendation)
        ↓
  Update AutomatedOrder
  ├─ status = 'created'
  ├─ created_order_id = order_code
  └─ Save
    ↓
  Return Response
  ├─ order_code (e.g., ORD-ABC12345)
  ├─ order_id
  └─ success message
    ↓
  Frontend
  ├─ Show success dialog
  ├─ Display order code
  └─ Redirect to order detail
    ↓
  User Confirms Order
  (usual payment flow)
```

---

## Database Schema Relationships

```
┌──────────────────────────┐
│      User                │
│ (existing model)         │
│ ├─ id (PK)               │
│ ├─ username              │
│ ├─ email                 │
│ └─ ...                   │
└──────────────────────────┘
        │ 1
        │
        ├─────────────────────────────────────┐
        │                                     │
        ↓ N                                   ↓ N
┌──────────────────────────┐    ┌────────────────────────────┐
│ ConversationSession      │    │ AutomatedOrder             │
│ (new)                    │    │ (new)                      │
│ ├─ id                    │    │ ├─ id                      │
│ ├─ session_id (unique)   │    │ ├─ status (draft/created)  │
│ ├─ context (JSON)        │    │ ├─ suggested_products(JSON)│
│ ├─ user_id (FK)          │    │ ├─ estimated_total         │
│ └─ created_at            │    │ ├─ created_order_id (FK)   │
└──────────────────────────┘    └────────────────────────────┘
        │ 1                              │ N
        │                               │
        ↓ N                             ↓
┌──────────────────────────┐    ┌─────────────────────┐
│ AIRecommendation         │    │ Order (existing)    │
│ (new)                    │    │ ├─ order_code       │
│ ├─ id                    │    │ ├─ user_id (FK)     │
│ ├─ product_id (FK) ─┐    │    │ ├─ status           │
│ ├─ confidence_score │    │    │ └─ total_amount     │
│ ├─ reason (reason)  │    │    └─────────────────────┘
│ └─ conversation_id  │    │
└──────────────────────────┘    ┌─────────────────────┐
                           │    │ Product (existing)  │
                           │    │ ├─ id               │
                           └───→│ ├─ name             │
                                │ ├─ price            │
                                │ └─ stock            │
                                └─────────────────────┘
```

---

## Component Tree: Frontend

```
AIAgentConsole (wrapper)
├── [STATE]
│   ├── conversationId
│   ├── showOrderPreview
│   ├── selectedRecommendations
│   └── isCreatingOrder
│
├── [Welcome Screen]
│   └── Button: "Bắt đầu tư vấn"
│
└── [Active Conversation]
    ├── AIAgentChat
    │   ├── [Messages Area]
    │   │   ├── User Messages (bubble style)
    │   │   ├── AI Messages (bubble style)
    │   │   └── Auto-scroll
    │   │
    │   ├── [Recommendations Area]
    │   │   └── List of products
    │   │
    │   └── [Input Area]
    │       ├── Textarea
    │       └── Send Button
    │
    └── [When showOrderPreview = true]
        └── OrderPreview
            ├── Item List
            ├── Total Price
            ├── Payment Method Select
            └── Confirm/Cancel Buttons
```

---

## API Contract Example

### Request
```json
{
  "method": "POST",
  "url": "/api/ai/conversations/1/send_message/",
  "headers": {
    "Authorization": "Bearer eyJ0eXAiOiJKV1QiLCJhbGc...",
    "Content-Type": "application/json"
  },
  "body": {
    "message": "Tôi muốn mua gấu bông cho con gái"
  }
}
```

### Response
```json
{
  "conversation_id": "session_abc123",
  "user_message": "Tôi muốn mua gấu bông cho con gái",
  "ai_response": "Tuyệt vời! Dựa trên nhu cầu...",
  "recommendations": [
    {
      "product_id": 1,
      "product_name": "Gấu bông Teddy 30cm",
      "reason": "Phù hợp cho trẻ em",
      "confidence_score": 0.85,
      "quantity": 1,
      "price": 450000
    }
  ],
  "should_create_order": false
}
```

---

## Technology Stack

```
┌─────────────────────────────────────┐
│         Frontend (Browser)           │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ React 18+ / Next.js             │ │
│ │ ├─ TypeScript                   │ │
│ │ ├─ CSS Modules                  │ │
│ │ └─ Fetch API                    │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
            ↓ HTTPS ↑
┌─────────────────────────────────────┐
│     Backend (Django Server)          │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ Django 5.0+                     │ │
│ │ ├─ Django REST Framework        │ │
│ │ ├─ JWT Authentication           │ │
│ │ └─ CORS Headers                 │ │
│ ├─────────────────────────────────┤ │
│ │ AI Services                     │ │
│ │ ├─ OpenAI API (gpt-3.5-turbo)   │ │
│ │ └─ Fallback Logic               │ │
│ ├─────────────────────────────────┤ │
│ │ Database ORM                    │ │
│ │ └─ Django ORM                   │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
            ↓ SQL ↑
┌─────────────────────────────────────┐
│      Database (MySQL/PostgreSQL)     │
│                                     │
│ ├─ ConversationSession table        │
│ ├─ AIRecommendation table           │
│ ├─ AutomatedOrder table             │
│ └─ [All existing tables]            │
└─────────────────────────────────────┘
```

---

## Admin Interface Flow

```
Admin User
    ↓
Login to Django Admin
    ↓
/admin/
    ↓
┌─ AI Agent Tư vấn
│  ├─ Phiên hội thoại AI
│  │  └─ View conversations, messages
│  │
│  ├─ Đề xuất sản phẩm AI
│  │  └─ View recommendations
│  │
│  └─ Đơn hàng tự động AI
│     └─ View draft orders
└─ [All existing admin panels]
```

---

## Deployment Architecture

```
┌─────────────────────────────────────┐
│      Load Balancer / CDN             │
└─────────────────────────────────────┘
        ↓↑
    ┌───┴──────────────────────┐
    ↓                          ↓
┌────────────────────┐  ┌──────────────────┐
│  Frontend Servers  │  │  Backend Servers │
│  (Next.js + Node)  │  │  (Django + Py)   │
│                    │  │                  │
│ npm start          │  │ gunicorn         │
└────────────────────┘  └──────────────────┘
                            ↓
                    ┌──────────────────┐
                    │  Redis Cache     │
                    │  (optional)      │
                    └──────────────────┘
                            ↓
                    ┌──────────────────┐
                    │  MySQL Database  │
                    │  (main storage)  │
                    └──────────────────┘
```

---

**This architecture is:**
- ✅ Scalable
- ✅ Maintainable
- ✅ Secure
- ✅ Performance-optimized
- ✅ Easy to extend
